.DS_Store
.env
.env.local
node_modules/
*.log
*.tmp
*.backup

# Python
.venv/
__pycache__/
*.pyc

# Local API-key helper scripts (interactive installers; never pushed)
add-*-key.sh
add-pipeline-keys.sh

# Pipeline run output (not committed)
website/test_output/

# Prototype scratch (not part of repo history)
newdesign/screenshots/
newdesign/uploads/

# verify_source.py local output
/out/verify/

# .claude/settings.local.json typically holds user-specific preferences
# (excluded by global gitignore). For news-v2 this file is required infra:
# it pre-approves permissions for the news-v2-autofix scheduled task.
# See docs/AUTOFIX-SCHEDULED-TASK.md.
!.claude/settings.local.json
